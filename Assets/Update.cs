using UnityEngine;

public class Update : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

    }

    public void UpdateMethod(string name)
    {
        Debug.Log("UpdateMethod:" + name);
    }
}
